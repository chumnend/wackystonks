import { generateName } from './generateName';
import { createPlayerInfo, getPlayerInfo, clearPlayerInfo } from './playerInfo';

export { generateName, createPlayerInfo, getPlayerInfo, clearPlayerInfo };
