import Spinner from '../../../common/Spinner';
import PageWrapper from '../../../common/PageWrapper';

const Loading = () => {
  return (
    <PageWrapper>
      <Spinner />
    </PageWrapper>
  );
};

export default Loading;
